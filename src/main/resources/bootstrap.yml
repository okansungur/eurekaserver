spring:
  application:
    name: eurekaserver
  cloud:
    config:
      uri: http://configserver:8071
      #uri: http://localhost:8071  #Local-Test
    loadbalancer:
      ribbon:
        enabled: false